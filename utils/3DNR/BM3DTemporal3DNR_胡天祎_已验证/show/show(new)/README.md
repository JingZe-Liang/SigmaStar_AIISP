# show(new)

这个目录汇总较新的 3DNR 展示材料，分成量化对比和 RGB 可视化两类。

子目录：

- `2DNRvs3DNR/`：保存 2DNR 与 3DNR 的量化指标表。
- `rgb域3DNR效果展示/`：保存多个 scene / ISO 的 RGB 域展示图。

这里的文件适合用于组会、汇报或人工检查算法趋势。若要追溯生成方法，应回到上层的 `3DNR.py`、`3DNR_to_rgb.py` 和 `notes.md`。
