视觉效果还是不错的，网络设计和监督方式也都有道理，但是主要有两个问题“
    1. 推理阶段以来MoG2的输出结果，部署的时候既需要我们的AI模块，又需要开源的运动监测模块，会被公司攻击设计冗余，并且增加计算量；
    2. 现在虽然网络参数非常小，但是现在的网络的输入的通道数非常多，有点担心推理阶段的显存占用

改进方向：
    1. 尝试将输入中的MoG2的分支拿掉；
    2. 尝试在主网络接一个轻量head，输出准确的motion map；
    3. 尝试减少主网络的输入通道数
    4. 将第二版网络的相关内容放到FusionModule/GateNet_LSY/Stage2；